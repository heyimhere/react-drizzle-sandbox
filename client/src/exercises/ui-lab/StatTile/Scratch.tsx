import ScratchPlaceholder from '../_shared/ScratchPlaceholder'

export default function Scratch() {
  return <ScratchPlaceholder message="Build a 2x2 grid of stat tiles. Label, big number, delta chip, inline SVG sparkline." />
}
