export default function Scratch() {
  return (
    <div className="text-center text-sm" style={{ color: 'var(--text)' }}>
      <p className="font-mono text-xs tracking-wider uppercase mb-2" style={{ color: 'var(--accent)' }}>
        your turn
      </p>
      <p>Rebuild the brutalist feed. Fetch from <code>jsonplaceholder.typicode.com/posts?userId=N</code>. User filter chips, chunky cards.</p>
      {/* Your code goes here */}
    </div>
  )
}
