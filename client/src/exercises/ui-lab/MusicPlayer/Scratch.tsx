import ScratchPlaceholder from '../_shared/ScratchPlaceholder'

export default function Scratch() {
  return <ScratchPlaceholder message="Build a now-playing card. Album art, track + artist, scrubber, play/skip controls. Bonus: gradient backdrop derived from artwork." />
}
