import ScratchPlaceholder from '../_shared/ScratchPlaceholder'

export default function Scratch() {
  // wipe everything below and rebuild
  return <ScratchPlaceholder message="Rebuild the full pastel dashboard composition. Pill nav, icon rail, greeting headline, mini chart, friends row, course card." />
}
