import ScratchPlaceholder from '../_shared/ScratchPlaceholder'

export default function Scratch() {
  return <ScratchPlaceholder message="Build a month-view calendar grid. Weekday header, prev/next nav, dim out-of-month days, highlight today, dot for event days." />
}
