import ScratchPlaceholder from '../_shared/ScratchPlaceholder'

export default function Scratch() {
  return <ScratchPlaceholder message="Build a settings page. Left nav, right pane with grouped toggle rows. Active nav item highlighted." />
}
