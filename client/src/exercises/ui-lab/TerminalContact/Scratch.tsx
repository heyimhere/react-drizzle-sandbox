import ScratchPlaceholder from '../_shared/ScratchPlaceholder'

export default function Scratch() {
  // wipe everything below and rebuild
  return <ScratchPlaceholder message="Rebuild the terminal contact form. macOS chrome, line numbers, blinking caret, green run button." />
}
