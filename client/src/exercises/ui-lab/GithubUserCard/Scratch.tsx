export default function Scratch() {
  return (
    <div className="text-center text-sm" style={{ color: 'var(--text)' }}>
      <p className="font-mono text-xs tracking-wider uppercase mb-2" style={{ color: 'var(--accent)' }}>
        your turn
      </p>
      <p>Rebuild the GitHub user lookup. Fetch from <code>api.github.com/users/&lt;login&gt;</code>. Loading skeleton, error panel, success card.</p>
      {/* Your code goes here */}
    </div>
  )
}
