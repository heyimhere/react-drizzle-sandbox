import ScratchPlaceholder from '../_shared/ScratchPlaceholder'

export default function Scratch() {
  // wipe everything below and rebuild
  return <ScratchPlaceholder message="Rebuild the three-column kanban. Cream surface, pastel column tints, paper cards with tags + avatars." />
}
