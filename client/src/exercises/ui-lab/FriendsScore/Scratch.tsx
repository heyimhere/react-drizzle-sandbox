import ScratchPlaceholder from '../_shared/ScratchPlaceholder'

export default function Scratch() {
  // wipe everything below and rebuild
  return <ScratchPlaceholder message="Rebuild the friends score card. Period dropdown, avatar rows, animated black progress bars, bold serif percentages." />
}
