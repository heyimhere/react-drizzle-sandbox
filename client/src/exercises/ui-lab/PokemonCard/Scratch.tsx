import ScratchPlaceholder from '../_shared/ScratchPlaceholder'

export default function Scratch() {
  return <ScratchPlaceholder message="Hit pokeapi.co/api/v2/pokemon/{id}. Show artwork, types, stats bars. Prev/Next to navigate." />
}
