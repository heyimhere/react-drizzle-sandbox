import ScratchPlaceholder from '../_shared/ScratchPlaceholder'

export default function Scratch() {
  return <ScratchPlaceholder message="Build a chat shell. Sidebar of conversations, message thread with bubbles, composer pinned to bottom." />
}
