import ScratchPlaceholder from '../_shared/ScratchPlaceholder'

export default function Scratch() {
  return <ScratchPlaceholder message="Fetch from open-meteo.com (no key). Show current temp + 7-day forecast strip. Handle loading and error states." />
}
