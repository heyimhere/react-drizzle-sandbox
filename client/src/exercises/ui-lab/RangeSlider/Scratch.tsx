import ScratchPlaceholder from '../_shared/ScratchPlaceholder'

export default function Scratch() {
  return <ScratchPlaceholder message="Build a custom range slider. Track, gradient fill, glowing thumb, live value label." />
}
