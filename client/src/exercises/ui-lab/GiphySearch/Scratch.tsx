import ScratchPlaceholder from '../_shared/ScratchPlaceholder'

export default function Scratch() {
  return <ScratchPlaceholder message="Debounced GIF search. Masonry grid (CSS grid + variable row span). Lazy-load images. Replace with real Giphy/Tenor key if you have one." />
}
