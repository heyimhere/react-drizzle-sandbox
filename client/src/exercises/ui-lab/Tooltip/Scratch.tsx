import ScratchPlaceholder from '../_shared/ScratchPlaceholder'

export default function Scratch() {
  return <ScratchPlaceholder message="Build a tooltip wrapper. Show on hover/focus with a delay, fade in, position above with a tiny arrow." />
}
