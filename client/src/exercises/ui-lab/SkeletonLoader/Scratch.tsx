import ScratchPlaceholder from '../_shared/ScratchPlaceholder'

export default function Scratch() {
  return <ScratchPlaceholder message="Build skeleton placeholders for a chat row — avatar circle, two text lines. Add a shimmer animation." />
}
