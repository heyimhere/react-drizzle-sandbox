import ScratchPlaceholder from '../_shared/ScratchPlaceholder'

export default function Scratch() {
  // wipe everything below and rebuild
  return <ScratchPlaceholder message="Rebuild the magazine-style feed. Editorial serif headlines, mono tags, scroll-triggered append." />
}
