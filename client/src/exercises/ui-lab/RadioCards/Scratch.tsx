import ScratchPlaceholder from '../_shared/ScratchPlaceholder'

export default function Scratch() {
  return <ScratchPlaceholder message="Build a vertical stack of radio cards. One selected at a time, with a glowing border on the chosen one." />
}
