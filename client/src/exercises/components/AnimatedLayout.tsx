export default function AnimatedLayout() {
  return (
    <div style={{ padding: '2rem' }}>
      <p style={{ color: 'var(--text-muted)' }}>Animated Layout component — replace with your work.</p>
    </div>
  )
}
