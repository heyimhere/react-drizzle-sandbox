export default function Accordion() {
  return (
    <div style={{ padding: '2rem' }}>
      <p style={{ color: 'var(--text-muted)' }}>Accordion component — replace with your work.</p>
    </div>
  )
}
