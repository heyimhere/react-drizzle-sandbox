export default function Dropdown() {
  return (
    <div style={{ padding: '2rem' }}>
      <p style={{ color: 'var(--text-muted)' }}>Dropdown component — replace with your work.</p>
    </div>
  )
}
